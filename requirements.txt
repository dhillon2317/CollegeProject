# Use pre-built wheels for faster installation
--find-links https://download.pytorch.org/whl/torch_stable.html
--find-links https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.5.0/en_core_web_sm-3.5.0-py3-none-any.whl

# Core dependencies
flask==2.0.1
gunicorn==20.1.0
flask-cors==3.0.10
python-dotenv==0.19.0

# Data processing
numpy==1.23.5  # Pre-built wheel available
pandas==1.5.3  # Pre-built wheel available

# ML dependencies
scikit-learn==1.2.2  # Pre-built wheel available
joblib==1.1.0

# spaCy and its dependencies with specific versions that have pre-built wheels
spacy==3.5.3
en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.5.0/en_core_web_sm-3.5.0.tar.gz

# Optimized build flags to prevent source compilation
export BLIS_ARCH=generic
export BLIS_COMPILER=gcc
